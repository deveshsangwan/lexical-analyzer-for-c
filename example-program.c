#include <stdio.h>
// main function
int main(){
	int i;
	double t = 7.2, arr[10];
	string a = "Hello!";
	// statements
	for(i = 0; i < 10; i++){   /* for loop */
    	ans[i] = solve(val, i);
    	val = res[i]; 
    	print(res[i]);
    	print('\n');
	}
}
return;
// functions
double solve (double x, int i){    
    // declarations
    double ans;
    // statements
    ans = x*i + i;
    return ans;
}